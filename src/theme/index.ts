import {createTheme} from '@rneui/themed';

export const theme = createTheme({
  lightColors: {
    primary: '#242458',
  },
  darkColors: {
    primary: '#242458',
  },
  components: {
    Button: {
      raised: true,
    },
  },
});
