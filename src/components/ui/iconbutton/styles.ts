import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  buttonStyles: {
    paddingVertical: 14,
    borderRadius: 50,
    fontWeight: 'bold',
    backgroundColor: '#414185',
    // height: 40,
    // width: 40,
  },
  containerStyle: {
    borderRadius: 50,
  },
});
