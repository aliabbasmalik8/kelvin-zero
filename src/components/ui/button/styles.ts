import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  buttonStyles: {
    paddingVertical: 14,
    borderRadius: 50,
    fontWeight: 'bold',
  },
  containerStyle: {
    borderRadius: 50,
    width: '100%',
  },
  titleStyle: {
    fontSize: 16,
    fontWeight: 'bold',
  },
});
