import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  buttonStyles: {
    paddingVertical: 14,
    borderRadius: 50,
    fontWeight: 'bold',
    height: 52,
  },
  containerStyle: {
    borderRadius: 50,
  },
  titleStyle: {
    fontSize: 16,
    fontWeight: '700',
    lineHeight: 19.2,
  },
});
