import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  buttonStyles: {
    paddingVertical: 18,
    fontSize: 16,
    borderRadius: 50,
    fontWeight: 'bold',
  },
  containerStyle: {
    borderRadius: 50,
    width: '90%',
  },
});
